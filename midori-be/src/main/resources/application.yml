logging:
  level:
    com.midori: DEBUG
    org.springframework.security: DEBUG

# JMdict Full Dictionary Import Configuration
midori:
  jmdict:
    import:
      enabled: false
      batch-size: 500
      skip-existing: true

spring:
  profiles:
    active: local
  data:
    redis:
      host: ${SPRING_REDIS_HOST:localhost}
      port: ${SPRING_REDIS_PORT:6379}
  servlet:
    multipart:
      max-file-size: 500MB
      max-request-size: 550MB

  datasource:
    url: ${DATABASE_URL:jdbc:postgresql://aws-1-ap-southeast-1.pooler.supabase.com:5432/postgres?sslmode=require}
    username: ${DATABASE_USERNAME:postgres.clyuyvdaoprxrpmrcyhd}
    password: ${DATABASE_PASSWORD:}
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 10
      minimum-idle: 2
      connection-timeout: 30000
      idle-timeout: 600000
      max-lifetime: 1800000

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
        jdbc:
          batch_size: 50
        order_inserts: true
        order_updates: true

  mail:
    host: smtp.gmail.com
    port: 587
    username: ${SPRING_MAIL_USERNAME:dathatinh2005aa@gmail.com}
    password: ${SPRING_MAIL_PASSWORD:}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
          connectiontimeout: 60000
          timeout: 60000
          writetimeout: 60000

server:
  port: 8080

app:
  admin:
    bootstrap-enabled: true
    email: ${ADMIN_BOOTSTRAP_EMAIL:admin@midori.local}
    password: ${ADMIN_BOOTSTRAP_PASSWORD:}
  jwt:
    secret: local-dev-jwt-secret-key-for-midori-backend-at-least-32-chars
    access-token-expiration: 86400000
  token:
    email-verification-expiration: 60
    password-reset-expiration: 3600
  google:
    client-id: 65823123353-je7nstqt77f1l4d0576vijjaklrnfum9.apps.googleusercontent.com
  mail:
    enabled: true
    from: dathatinh2005aa@gmail.com
  frontend:
    base-url: http://localhost:8081

supabase:
  url: https://clyuyvdaoprxrpmrcyhd.supabase.co
  service-role-key: ${SUPABASE_SERVICE_ROLE_KEY:}
  storage:
    bucket: ${SUPABASE_STORAGE_BUCKET:listening-audios}
    videos-bucket: ${SUPABASE_VIDEOS_BUCKET:shadowing-videos}

# Groq API Keys
groq:
  api-keys: ${GROQ_API_KEYS:${GROQ_API_KEY_1:},${GROQ_API_KEY_2:}}
  api-key: ${GROQ_API_KEY:${groq.api-keys}}

# AI Configuration
ai:
  provider: ${AI_PROVIDER:gemini}
  provider-order: ${AI_PROVIDER_ORDER:gemini,openrouter}
  timeout-seconds: 120
  temperature: 0.1
  max-tokens: 8192
  executor:
    core-pool-size: ${AI_EXECUTOR_CORE_POOL_SIZE:1}
    max-pool-size: ${AI_EXECUTOR_MAX_POOL_SIZE:1}
    queue-capacity: ${AI_EXECUTOR_QUEUE_CAPACITY:100}
  gemini:
    api-key: ${GEMINI_API_KEY:}
    api-keys: ${GEMINI_API_KEYS:${GEMINI_API_KEY_1:},${GEMINI_API_KEY_2:},${GEMINI_API_KEY_3:}}
    model: ${GEMINI_MODEL:gemini-flash-latest}
    fallback-models: ${GEMINI_FALLBACK_MODELS:gemini-2.0-flash,gemini-2.0-flash-lite}
    base-url: https://generativelanguage.googleapis.com
    enabled: true
  openai:
    api-key: ${OPENAI_API_KEY:}
    model: gpt-4o
    base-url: https://api.openai.com/v1
    enabled: false
  deepseek:
    api-key: ${DEEPSEEK_API_KEY:}
    model: deepseek-chat
    base-url: https://api.deepseek.com/v1
    enabled: false
  openrouter:
    api-key: ${OPENROUTER_API_KEY:}
    api-keys: ${OPENROUTER_API_KEYS:${OPENROUTER_API_KEY:}}
    models: ${OPENROUTER_MODEL:google/gemma-4-31b-it:free}
    fallback-models: ${OPENROUTER_FALLBACK_MODELS:google/gemma-4-26b-a4b-it:free,openai/gpt-oss-20b:free}
    # Per-call timeouts (ms)
    chat-timeout-ms: 45000
    quiz-timeout-ms: 60000
    connect-timeout-ms: 5000
    chat-max-tokens: 4096
    quiz-max-tokens: 4096
    referer: ${AI_OPENROUTER_REFERER:http://localhost:8081}
  pdf:
    max-file-size: 10485760
    async-threshold: 1048576
  ocr:
    enabled: true
    simulated: true
  fallback-enabled: false

# FFmpeg
ffmpeg:
  path: ${FFMPEG_PATH:}
  probe-path: ${FFPROBE_PATH:}

# Shadowing
shadowing:
  speech:
    provider: ${SHADOWING_SPEECH_PROVIDER:groq}
    profile: ${SHADOWING_SPEECH_PROFILE:balanced}
    api-timeout-seconds: ${SHADOWING_SPEECH_API_TIMEOUT_SECONDS:120}
    models:
      fast: ${SHADOWING_SPEECH_MODEL_FAST:whisper-large-v3-turbo}
      balanced: ${SHADOWING_SPEECH_MODEL_BALANCED:whisper-large-v3}
      accurate: ${SHADOWING_SPEECH_MODEL_ACCURATE:whisper-large-v3}
  evaluation:
    similarity-threshold: ${SHADOWING_SIMILARITY_THRESHOLD:0.80}
    gemini-threshold: ${SHADOWING_GEMINI_THRESHOLD:80}
    max-recording-duration-seconds: ${SHADOWING_MAX_RECORDING_SECONDS:15}
    gemini-model: ${SHADOWING_GEMINI_MODEL:gemini-2.0-flash}
    api-timeout-seconds: ${SHADOWING_API_TIMEOUT_SECONDS:120}
    cache-ttl-minutes: ${SHADOWING_CACHE_TTL_MINUTES:60}
    cache-max-size: ${SHADOWING_CACHE_MAX_SIZE:10000}
